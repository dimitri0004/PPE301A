<form method="post" action="{{ path('app_emploi_temps_delete', {'id': emploi_temp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emploi_temp.id) }}">
    <button class="btn">Delete</button>
</form>
