{% extends 'base.html.twig' %}

{% block title %}Ajouter eleves{% endblock %}

{% block body %}
<center><h3>Mofier un eleve</h3></center>
    <div class=" justify-content-center align-items-center" style="width: 600px; margin-left:  400px;">
        {{ form_start(form) }}
            {{ form_row(form.nom) }}
            {{ form_row(form.prenom) }}
            {{ form_row(form.email) }}
            {{ form_row(form.numero) }}
            <center><button type="submit" class="btn btn-primary">modifier</button></center>
        {{ form_end(form) }}
    </div>
{% endblock %}
