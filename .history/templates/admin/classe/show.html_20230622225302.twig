{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ classe.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ classe.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_classe_liste') }}">back to list</a>

    <a href="{{ path('admin_classe_modifier', {'id': classe.id}) }}">edit</a>

    {{ include('admin/classe/_delete_form.html.twig') }}
{% endblock %}
