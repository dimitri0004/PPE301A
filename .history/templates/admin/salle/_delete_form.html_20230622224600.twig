<form method="post" action="{{ path('admin_salle_supprimer', {'id': salle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle.id) }}">
    <button class="btn">Delete</button>
</form>
