{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ salle.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ salle.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_salle_liste') }}">back to list</a>

    <a href="{{ path('admin_salle_modifier', {'id': salle.id}) }}">edit</a>

    {{ include('admin/salle/_delete_form.html.twig') }}
{% endblock %}
