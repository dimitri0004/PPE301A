{% extends 'base.html.twig' %}

{% block title %}Anne{% endblock %}

{% block body %}
    <h1>Anne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ anne.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ anne.code }}</td>
            </tr>
            <tr>
                <th>Anne</th>
                <td>{{ anne.anne ? anne.anne|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_anne_index') }}">back to list</a>

    <a href="{{ path('app_anne_edit', {'id': anne.id}) }}">edit</a>

    {{ include('anne/_delete_form.html.twig') }}
{% endblock %}
