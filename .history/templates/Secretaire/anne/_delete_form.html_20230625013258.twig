<form method="post" action="{{ path('app_anne_delete', {'id': anne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ anne.id) }}">
    <button class="btn">Delete</button>
</form>
