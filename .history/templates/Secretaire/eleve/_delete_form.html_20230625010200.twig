<form method="post" action="{{ path('secretaire_eleve_supprimer', {'id': eleve.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eleve.id) }}">
    <button class="btn">Delete</button>
</form>
